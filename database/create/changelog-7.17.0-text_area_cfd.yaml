databaseChangeLog:

  - changeSet:
      id: 7.17.0-text_area_cfd
      author: omp
      comment: create 'text_area_cfd' table
      context: create
      dbms: postgresql, mysql

      changes:
        - createTable:
            tableName: text_area_cfd

            columns:
              - column:
                  name: id
                  type: bigint
                  constraints:
                      nullable: false
                      primaryKey: true
                      primaryKeyName: text_area_cfdPK


  - changeSet:
      id: 7.17.0-text_area_cfd
      author: omp
      comment: create 'text_area_cfd' table
      context: create
      dbms: oracle, mssql

      changes:
        - createTable:
            tableName: text_area_cfd

            columns:
              - column:
                  name: id
                  type: number(19,0)
                  constraints:
                      nullable: false
                      primaryKey: true
                      primaryKeyName: text_area_cfdPK

